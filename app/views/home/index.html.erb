<header style="display:flex;gap:12px;align-items:center;justify-content:space-between;padding:8px 12px;border-bottom:1px solid #eee;">
  <div>
    ようこそ、<strong><%= current_user.email %></strong> さん
  </div>
  <nav style="display:flex;gap:8px;">
    <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
  </nav>
</header>

<main style="padding:24px;">
  <h1 style="margin-bottom:16px;">ホーム</h1>

  <div style="display:flex; gap:12px; flex-wrap:wrap;">
    <%= link_to "カレンダー", calendar_index_path, class: "btn-primary" %>
    <%= link_to "計測", new_tracked_session_path, class: "btn-primary" %>
    <%= link_to "カテゴリー作成", categories_path, class: "btn-primary" %>
  </div>
</main>
